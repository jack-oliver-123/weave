## 1. 验收夹具与依赖边界

- [x] 1.1 添加 `unified`、`remark-parse`、`remark-gfm`、`string-width` 与 `cli-highlight` 的直接依赖并更新锁文件，记录 Node.js 22、ESM 和许可证兼容性检查结果
- [x] 1.2 先为渐进式 Markdown 编写失败的验收测试，覆盖逐字符分片、未闭合尾段、最终规范化、完整元素集、纯文本降级和 ANSI 清理边界
- [x] 1.3 先为实际显示行编写失败的验收测试，覆盖 ASCII、中文、Emoji、组合字符、软折行、列表缩进、终端变宽/变窄和光标坐标
- [x] 1.4 先为滚轮与稳定阅读锚点编写失败的验收测试，覆盖上/下滚轮、未读实际行数、`Ctrl+End`、流式追加和唯一滚动区域
- [x] 1.5 先为消息队列状态机编写失败的验收测试，覆盖连续入队、空行合并、正常续发、四类暂停终态、显式恢复、草稿隔离、`Ctrl+Z` 和退出提示
- [x] 1.6 扩展确定性假流夹具与终端验收样例，包含长 Markdown、宽/窄表格、长代码、Unicode 输入、滚轮原始序列和多条排队消息

## 2. Markdown 与实际显示行模型

- [x] 2.1 实现基于 `Intl.Segmenter` 与 `string-width` 的字素遍历、显示宽度、折行、缩进、截断和光标列纯函数
- [x] 2.2 定义带稳定源锚点和样式 span 的语义块与 `DisplayRow` 契约，并让纯文本转录先通过该模型保持现有行为
- [x] 2.3 实现 GFM AST 解析和稳定前缀/未闭合尾段分类，按渲染帧合并增量、缓存稳定块并在终态完成最终解析
- [x] 2.4 实现标题、段落、强调、删除线、列表、任务列表、引用、行内代码、链接和分隔线到终端语义块的映射
- [x] 2.5 实现表格宽度测量、宽终端对齐布局和窄终端逐记录键值降级，并验证不产生横向滚动区
- [x] 2.6 实现围栏代码语言标签、内部 SGR 白名单语法高亮、未知语言降级和带 `↪` 的超宽行视觉折行
- [x] 2.7 将 assistant 转录切换到渐进式 Markdown 显示行，同时保持原始安全文本、turn 状态、耗时和解析失败纯文本降级

## 3. 布局、滚动与输入框

- [x] 3.1 重构布局计算，使对话区、composer 和队列摘要按实际显示行分配高度，输入过长时保持光标窗口可见且不出现第二滚动条
- [x] 3.2 将视口从逻辑行偏移升级为稳定显示行锚点，正确处理流式新增、Markdown 块重排和终端尺寸变化
- [x] 3.3 增加“正在查看上文 · 新增 N 行 · Ctrl+End 返回底部”状态，并在回到底部后清零和恢复自动跟随
- [x] 3.4 扩展 composer 按字素边界执行左右移动、退格、删除、Home/End、粘贴和多行自动折行，避免拆分 Emoji 或组合字符

## 4. 真实光标与鼠标滚轮

- [x] 4.1 移除手绘 `█`，使用 Ink `useCursor` 按统一显示行坐标定位原生光标，并覆盖过小终端、调整尺寸和卸载恢复
- [x] 4.2 建立滚轮终端探针，分别验证 Windows 11 CMD、PowerShell/Windows Terminal 与 WSL 的鼠标模式启停、原始事件格式和清理行为
- [x] 4.3 实现终端输入适配器，成对启停鼠标跟踪、吞掉完整或不完整控制序列并只向视口分发上/下滚轮动作
- [x] 4.4 保留 `PageUp`、`PageDown` 和 `Ctrl+End` 等价路径，并验证滚轮在终端任意位置只滚动对话区

## 5. 生成期消息队列

- [x] 5.1 扩展纯 `TuiState` reducer，建模队列项、活动/暂停状态、最近反馈、队尾撤回和退出丢失计数
- [x] 5.2 将生成期间 `Enter` 接到入队动作，原子清空已入队 composer，并保证 `ConversationController` 始终只有一个活动 turn
- [x] 5.3 实现正常完成后的队列不可变快照、按 `\n\n` 合并与单次自动续发，确保未入队草稿不被读取或清空
- [x] 5.4 实现错误、中断、截断和拒答后的暂停，以及暂停状态下 `Enter` 追加可选草稿并显式恢复发送
- [x] 5.5 实现固定一行的“数量 + 最新预览”、`Ctrl+Z` 恢复队尾且不覆盖草稿、以及 `Ctrl+C` 二次退出丢失提示
- [x] 5.6 增加异步竞态测试，验证终态与入队同时发生、快速连续提交、撤回和退出时不会重复发送或丢失未提交草稿

## 6. 集成与验证

- [x] 6.1 运行 Markdown、显示行、viewport、composer、队列和终端控制聚焦单元测试，并修复所有发现的问题
- [x] 6.2 扩展 Ink 集成测试，验证 Markdown 样式结构、固定区域、单一滚动区、队列摘要和真实光标坐标
- [x] 6.3 扩展 Windows 确定性 E2E，通过 `cmd.exe` 与 PowerShell/Windows Terminal 验证 Markdown、实际行、滚轮、队列、尺寸变化和终端恢复
- [x] 6.4 扩展 WSL `tmux` 确定性 E2E，验证等价的 Markdown、布局、键盘/鼠标滚动、队列和退出恢复场景
- [x] 6.5 按固定清单执行 Windows 11 CMD 中文输入法人工 smoke，分别记录单行、多行（经典 Console Host 使用 `Ctrl+J`，Windows Terminal 1.25+ 使用 `Shift+Enter`）、自动折行、中英 Emoji 混合输入和退出恢复结果
- [x] 6.6 运行 `npm run typecheck`、`npm run build`、`npm test` 和现有默认 E2E，明确区分聚焦测试、全量套件、自动终端 gate 与人工 IME gate
- [x] 6.7 运行 `openspec validate improve-terminal-chat-experience --strict`，复核实现与 delta spec 一致后再进入同步和归档阶段
