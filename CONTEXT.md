# Weave Agent 安全领域

本上下文统一 Weave 在执行 Agent 动作时使用的安全术语，避免把计划流程、应用授权与操作系统隔离混为一谈。

## Language

**动作（Action）**：
Agent 提议执行的、可能跨越文件、进程、网络或数据边界的操作。
_Avoid_：指令、步骤

**动作提案（Action Proposal）**：
动作批次请求中由模型或宿主提交、等待动作网关标准化的受信类型提案；一个提案必须映射为一个不可拆分的标准化动作，且提案本身不携带授权。
_Avoid_：工具调用、已授权动作、任意动作信封

**标准化动作（Normalized Action）**：
使用版本化类型字段明确描述操作、规范化资源、参数、所需能力、数据出口和风险信息的动作表示，是权限规则唯一可以匹配的对象。
_Avoid_：命令文本、Prompt 片段

**能力清单（Capability Manifest）**：
标准化动作在执行前完整声明的不可变最小能力集合，首版只包含文件读取、文件写入、进程启动、网络出口、凭据代用、数据披露和 Memory 持久化七类封闭原语；任一能力未获准都会拒绝整个动作。
_Avoid_：权限列表、沙箱配置、可选能力

**能力原语（Capability Primitive）**：
动作网关、权限规则、能力票据和沙箱后端共同理解的版本化最小授权单位；业务操作语义保留在标准化动作中，设备访问、原始 IPC、提权等禁止能力不属于可授权原语。
_Avoid_：工具权限、操作名称、动态能力字符串

**动作授权（Action Authorization）**：
在动作执行前，根据安全策略决定该动作是否可以执行的过程。
_Avoid_：权限系统、Plan 审批

**动作网关（Action Gateway）**：
所有跨越文件、进程、网络、模型、持久存储或远程工具信任边界的能力必须进入的统一标准化与动作授权入口。
_Avoid_：工具权限中间件、分层自行授权

**动作任务会话（Action Task Session）**：
由动作网关为单个 Task 创建并跨多个 Run 复用的安全上下文，固定策略快照、权限模式、模型目标、资源配额与任务沙箱，并负责处理动作、接收结构化授权和关闭资源。
_Avoid_：权限会话、工具执行器、Run 沙箱

**网关请求（Gateway Request）**：
调用方向动作任务会话提交的封闭、版本化请求联合，首版只包含模型交换请求和动作批次请求；它不能携带授权裁决、能力票据或沙箱配置。
_Avoid_：任意动作信封、权限请求、执行命令

**模型交换请求（Model Exchange Request）**：
要求动作网关从带来源的上下文片段组装固定模型目标请求，并只释放经过输入与输出守卫检查的模型结果的网关请求。
_Avoid_：LLM 调用、Prompt 请求

**动作批次请求（Action Batch Request）**：
承载同一模型响应提出的一组受信类型动作提案、并要求在任一动作执行前完成整批授权预检的网关请求；本地工具、Memory 与未来扩展都通过其内部动作联合接入。
_Avoid_：工具执行请求、动态 Gateway Request 类型

**提案批次引用（Proposal Batch Reference）**：
动作网关为其私下保管的一批模型或可信 Adapter 动作提案签发、绑定 Task、Run、迭代、模型交换、授权纪元和批次摘要的短时单次不透明引用；AgentLoop 只能提交该引用，不能读取或改写原始参数。
_Avoid_：工具调用数组、批次 ID、可编辑动作列表

**能力裁剪（Capability Shaping）**：
根据当前策略减少模型可见的整体不可用工具或能力的提示优化；它只表示动作可以被提出，不能替代执行前授权。
_Avoid_：权限执行、Prompt 安全边界

**授权来源（Authorization Source）**：
能够产生授权的结构化可信通道，仅包括进程外可信配置、显式权限模式和结构化授权确认。用户消息、项目文件、工具结果、网页及模型输出等自然语言内容只能提出动作，不能证明动作已获授权。
_Avoid_：Prompt 授权、模型自行授权

**可信计算基（Trusted Computing Base）**：
安全承诺所依赖的宿主 OS、Weave 宿主安全层、结构化用户决定及已验证 sandbox backend；这些组件失陷不属于动作授权能够抵御的范围。
_Avoid_：所有本地组件、模型服务

**不可信内容（Untrusted Content）**：
可能影响 Agent 行为但不能产生授权的模型输出、自然语言、项目数据、工具结果及外部服务内容。
_Avoid_：恶意内容、已确认注入

**项目指令（Project Instructions）**：
工作区内描述代码规范或任务建议、但只能作为普通消息区不可信上下文提供给模型的项目文本；它不能进入 system 角色或改变授权。
_Avoid_：可信配置、系统指令

**Prompt 信任映射（Prompt Trust Mapping）**：
把版本固定的 Weave 协议与白名单结构化标量限制在高优先级 system 通道，并把用户输入、项目指令、Plan、历史、Memory、路径和外部内容放入保留来源的不可信通道的固定映射。
_Avoid_：Prompt 优先级、system 包装、来源分隔符

**授权裁决（Authorization Decision）**：
动作授权产生的 `allow`、`deny` 或 `ask` 结果；`ask` 表示必须取得人的明确决定后才能继续该动作。
_Avoid_：TaskDecision、计划决策

**命令风险检查（Command Risk Check）**：
防线决策链的第一层。它对极端危险动作产生不可被后续层覆盖的拒绝，或为其他动作附加风险信息后继续；未命中规则不代表动作已获允许。
_Avoid_：安全命令判定、未命中即允许的黑名单

**内置硬拒绝（Built-in Hard Denial）**：
由 Weave 版本定义、不可被规则、模式或授权确认覆盖的安全不变量，只用于阻止破坏安全平面、越出宿主边界、披露凭据或造成宿主级灾难的动作。
_Avoid_：高风险动作列表、用户 deny 规则

**结构化进程动作（Structured Process Action）**：
以解析后不可变执行身份、参数、工作目录和所需能力明确表达的命令动作，可被稳定标准化并参与权限规则匹配；授权后身份变化必须重新预检。
_Avoid_：Shell 命令字符串

**授权进程树（Authorized Process Tree）**：
由一个结构化进程动作启动、全部后代只能继承相同或更窄动作沙箱配置的完整进程树，是取消和资源核算的统一边界。
_Avoid_：根进程授权、子进程独立授权

**进程生命周期（Process Lifetime）**：
进程动作声明的 `action` 或 `task` 存活范围；默认 `action` 会在动作完成时终止完整进程树，`task` 必须显式获准且始终保留创建时的动作沙箱配置。
_Avoid_：后台进程、守护进程、无限存活

**原始 Shell 动作（Raw Shell Action）**：
包含管道、重定向、替换或脚本语法的 Shell 动作，始终视为高风险；只有仓库外精确规则可以免除授权确认。
_Avoid_：已解析安全命令

**路径能力边界（Path Capability Boundary）**：
Agent 启动时由可信配置声明的工作区根及其只读或读写能力。授权确认只能裁决边界内的动作，不能在运行中扩大可见的宿主路径。
_Avoid_：当前工作目录、HITL 临时挂载

**工作区写时复制视图（Workspace Copy-on-write View）**：
由宿主只读基线和 Task 私有变更层组成的 sandbox 工作区视图，使工具无法直接修改宿主文件身份或内容。
_Avoid_：工作区读写挂载、Task 全量副本

**工作区提交代理（Workspace Commit Broker）**：
在沙箱外按能力票据、路径边界及基线身份事务性应用授权变更集的宿主服务；并发冲突或越界变更不能覆盖用户数据。
_Avoid_：sandbox 直接写入、Task 结束覆盖

**事务性动作变更集（Transactional Action Change Set）**：
单个动作在私有写时复制层产生、经整批验证后在提交锁内使用持久化日志、逐路径原子替换和失败回滚应用的工作区变化；它保证 Weave 动作不会观察部分提交，但不承诺外部进程看不到跨文件切换的中间状态。
_Avoid_：跨文件瞬时原子提交、授权子集提交、执行后补授权

**工作区恢复冲突（Workspace Recovery Conflict）**：
事务恢复时目标路径既不匹配日志中的执行前状态也不匹配执行后状态、因而不能安全自动回滚或完成提交的状态；它会关闭工具模式并保留现场，直到用户对精确路径作出结构化恢复决定。
_Avoid_：提交失败、自动覆盖、脏工作区

**普通工作区数据（Ordinary Workspace Data）**：
工作区内可按任务需要发送给当前选定模型服务的非敏感项目数据。
_Avoid_：全部工作区数据

**数据分类（Data Classification）**：
由内置规则、用户策略、项目减权策略及内容检测共同产生并按 `credential > sensitive > ordinary` 单调合并的数据等级；任何来源都不能降低更高等级。
_Avoid_：检测器标签、可覆盖密级

**来源信封（Provenance Envelope）**：
内容片段随身携带的 `sourceId`、来源类型、数据分类、内容摘要和用途元数据；输入守卫、输出守卫及动作网关只能组合或提高这些事实，不能丢失来源后重新推断为普通数据。
_Avoid_：消息 metadata、审计记录、可信标签

**安全上下文账本（Secure Context Ledger）**：
动作任务会话内保存内容引用、来源信封、分类和出口授权状态的 Task 私有模型上下文真相源；模型原始输出、动作提案和工具原始结果不能绕过输入与输出守卫离开该账本。
_Avoid_：ConversationStore、聊天记录、Prompt 缓存

**公开转录（Public Transcript）**：
只保存已允许在本地交互界面显示的脱敏用户文本、模型文本和安全摘要的会话视图；它不保存私有账本内容，也不能直接作为模型请求或恢复旧授权。
_Avoid_：模型历史、完整会话、Secure Context Ledger

**分类传播（Classification Propagation）**：
派生内容继承其全部输入中的最高数据分类的规则；模型、Shell、进程和未知扩展属于不透明变换，只有由可信 Adapter 独立构造且不包含输入内容的结构化字段可以单独分类。
_Avoid_：自动降密、模型脱敏、输出重新检测

**敏感数据（Sensitive Data）**：
由可信规则标记、在发送给模型或其他出口前必须取得授权确认的数据。
_Avoid_：凭据数据、普通项目数据

**敏感内容任务授权（Task-scoped Sensitive Content Grant）**：
允许同一敏感内容在当前 Task 内按固定用途重复发送到同一模型目标的出口授权，绑定内容摘要哈希且不传递给派生内容。
_Avoid_：敏感数据通行证、派生内容授权

**凭据数据（Credential Data）**：
私钥、真实环境密钥、访问令牌、云凭据及认证环境变量等不得进入模型上下文、日志或普通错误信息的数据。
_Avoid_：可确认的敏感数据

**Prompt 注入风险（Prompt Injection Risk）**：
不可信自然语言内容试图改变 Agent 行为或伪造授权的风险标记；检测只会提高动作风险，不能授予权限或替代运行时边界。
_Avoid_：Prompt 注入判定、已阻止注入

**受控网络出口（Controlled Network Egress）**：
OS 沙箱内工具访问网络的唯一通道，只允许按授权裁决访问明确的协议、目标和端口。授权确认可以批准具体目标，但不能为沙箱开放原始网络能力。
_Avoid_：临时开网、命令级网络提醒

**网络目标（Network Destination）**：
由规范化协议、主机、端口及实际解析地址共同确定的出口资源；重定向、地址类别变化或 origin 变化都会形成新的待裁决目标。
_Avoid_：原始 URL、域名字符串

**凭据代理（Credential Broker）**：
在沙箱外持有原始凭据，并只按已授权目标和操作范围代表工具完成认证的宿主服务；沙箱、模型和日志只能引用凭据标识。
_Avoid_：环境变量注入、密钥挂载

**凭据标识（Credential Reference）**：
用户安全策略和 Provider 配置用于引用 OS 凭据库中秘密值的非敏感名称；它不包含也不能推导原始凭据。
_Avoid_：API Key、环境变量值

**Memory 持久化能力（Memory Persist Capability）**：
允许已分类数据按明确存储范围和键写入会影响后续 Task 的 Memory 的能力原语；它不能被普通文件写入授权隐式覆盖。
_Avoid_：ConversationStore 写入、文件写入、会话历史

**权限规则（Permission Rule）**：
针对特定动作及其资源匹配条件产生授权裁决的规则。
_Avoid_：工具开关、调度模式

**允许覆盖（Allow Coverage）**：
可信 `allow` 规则对其明确匹配的单项能力提供的授权证明；完整能力清单可以由多条规则共同覆盖，但动作类型命中或部分覆盖不能隐式允许其余能力。
_Avoid_：动作白名单、工具放行、整条规则命中

**用户安全策略（User Security Policy）**：
位于工作区外且通过宿主所有权、ACL 和文件身份验证，可设置默认模式、资源预算并授予或限制能力的可信结构化配置；其中只能引用凭据标识，不能保存原始凭据。
_Avoid_：Provider 配置、项目权限规则

**项目权限规则（Project Permission Rule）**：
仓库携带的结构化权限规则，只能拒绝动作或要求授权确认，不能授予能力或覆盖仓库外的限制。
_Avoid_：项目授权、README 权限说明

**规则合并（Rule Combination）**：
同一标准化动作命中的全部权限规则按 `deny > ask > allow > no_match` 合并，结果不受规则或来源加载顺序影响。
_Avoid_：首条命中、后规则覆盖

**能力裁决合并（Capability Decision Folding）**：
将动作级裁决与能力清单中每项能力的裁决按 `deny > ask > allow` 合并为整个动作的最终裁决；只有全部为 `allow` 才能签发能力票据，HITL 也只能决定完整动作。
_Avoid_：部分授权、能力多数决、动作级 allow 覆盖

**策略快照（Policy Snapshot）**：
一个 Task 固定使用的已验证权限规则与权限模式版本。活动 Task 只接受即时收权，任何扩权仅对后续 Task 生效。
_Avoid_：实时扩权、可变任务策略

**权限模式（Permission Mode）**：
为一组动作规定默认授权姿态的用户可选策略组合。
_Avoid_：Agent 模式、Plan 模式

**只读模式（read_only）**：
默认允许普通工作区读取与普通数据向固定模型或终端披露，拒绝写入、进程、网络、凭据代用和 Memory 持久化，并对敏感数据要求授权确认的权限模式。
_Avoid_：安全模式

**监督模式（supervised）**：
默认允许普通工作区读取与普通数据向固定模型或终端披露，并对写入、进程、网络、凭据代用、Memory 持久化及敏感数据要求授权确认的默认权限模式。
_Avoid_：默认允许模式

**自主模式（autonomous）**：
默认允许普通工作区读写、本地结构化进程以及普通数据向固定模型或终端披露，但仍对 Raw Shell、高风险进程、网络、凭据代用、Memory 持久化和敏感数据要求授权确认的权限模式。
_Avoid_：full_access、无权限模式

**授权确认（Authorization Confirmation）**：
人在回路中对一个待确认动作作出的明确允许或拒绝；它不批准计划，也不扩大未展示动作的授权范围。
_Avoid_：Plan 审批、普通用户输入

**知情授权（Informed Authorization）**：
用户基于安全层生成的标准化动作、实际资源、能力、风险和作用范围作出的结构化授权；模型生成的理由不能替代这些事实。
_Avoid_：模型摘要确认、泛化同意

**授权请求（Authorization Request）**：
在唯一转录区内展示并阻塞当前工具批次的结构化待确认动作；其固定操作栏不形成独立滚动区域。
_Avoid_：权限弹窗、普通提问

**待决授权（Pending Authorization）**：
ActiveRun 中唯一尚未解决、绑定 `taskId + runId + requestId + authorizationEpoch + actionDigest` 的授权请求；只有覆盖全部待决动作的匹配结构化决定才能解决它。
_Avoid_：待回答问题、普通用户输入、权限队列

**单次授权（One-time Grant）**：
只允许一个已标准化且绑定当前 `callId` 的待确认动作执行一次的授权。
_Avoid_：临时规则

**任务授权（Task-scoped Grant）**：
只在当前 `taskId` 内对安全层生成并明确展示的窄动作范围有效的授权；模型不能定义或扩大该范围。
_Avoid_：会话授权、永久授权

**授权纪元（Authorization Epoch）**：
Task 内绑定任务授权有效上下文的单调版本；任何新的自然语言输入或 Plan 版本都会推进纪元并撤销旧任务授权。
_Avoid_：Plan 版本、策略版本

**批次授权预检（Batch Authorization Preflight）**：
模型产生的一批动作在任何一个开始执行前完成标准化和授权裁决，并集中处理其中所有待确认动作；每项必须获得明确决定，不能无范围地批量允许。
_Avoid_：执行中确认、先读后确认

**任务拒绝记忆（Task-scoped Denial）**：
用户拒绝的标准化动作在当前 `taskId` 内保持拒绝；相同动作不再触发授权确认，实质目标或参数变化后按新动作重新裁决。
_Avoid_：永久拒绝、任务终止

**授权中断（Authorization Interruption）**：
授权交互取消或链路异常导致未决动作及票据失效的状态；它取消当前 Run，但不等同于用户明确拒绝，也不写入任务拒绝记忆。
_Avoid_：权限拒绝、默认拒绝

**授权暂停（Authorization Suspension）**：
同一 ActiveRun 在批次预检后等待结构化授权决定的暂停状态；等待保留原异步流，不创建新 Run、不请求模型，也不执行任何工具，普通用户输入不能解除暂停。
_Avoid_：等待用户输入、Run 停止

**防线决策链（Defense Decision Chain）**：
动作在执行前依次经过命令风险检查、路径能力边界、权限规则、权限模式和授权确认五层判断的应用安全链。前两层的拒绝不可覆盖，显式规则优先于权限模式，只有 `ask` 进入授权确认；获准动作仍必须在 OS 沙箱内执行。
_Avoid_：单一权限检查

**OS 沙箱（OS Sandbox）**：
由操作系统强制限制动作可访问的文件、进程和网络能力的执行边界；它不依赖 Agent 遵守提示词或应用层判断正确。
_Avoid_：工作区初始目录、路径字符串校验

**沙箱执行器（Sandbox Runner）**：
由沙箱监督器、任务沙箱和动作工作进程组成、独占全部业务工具真实资源访问的隔离执行子系统；宿主 Agent 进程不直接读写工作区、启动工具进程或访问工具网络。
_Avoid_：Shell 包装器、宿主工具调度器、长驻工具进程

**沙箱监督器（Sandbox Supervisor）**：
验证动作网关身份与能力票据、维护任务沙箱并为每个动作创建最小权限工作进程的可信控制面；它不执行模型生成的业务代码，也不向工作进程暴露控制通道或票据签名材料。
_Avoid_：工具进程、AgentLoop、沙箱内守护进程

**任务沙箱（Task Sandbox）**：
绑定单个 `taskId`、供该 Task 的多个 Run 使用并保存写时复制视图、资源账本及获准长驻进程的 OS 隔离环境；Task 结束后销毁全部运行态资源，但不回滚已授权的工作区修改。
_Avoid_：会话沙箱、逐动作虚拟机

**动作工作进程（Action Worker）**：
沙箱监督器按单张能力票据和动作沙箱配置创建、默认在动作完成后连同进程树一起销毁的一次性执行单元；它不能读取策略、审计库、原始凭据、签名密钥或监督器控制通道。
_Avoid_：Task Worker、长驻 Runner、工具宿主

**任务资源配额（Task Resource Budget）**：
Task 创建时固定、由 OS 沙箱强制执行的 CPU、内存、进程、时间、临时磁盘和输出上限；授权确认不能提高配额。
_Avoid_：工具超时、权限额度

**能力票据（Capability Ticket）**：
宿主安全层为一个已授权标准化动作签发的短时、单次消费、窄范围执行凭据，绑定 runner、sandbox、Task、Run、调用、动作与能力摘要、策略版本、授权纪元、撤销版本和 nonce；沙箱监督器必须重新标准化并验签后原子消费。
_Avoid_：会话令牌、通用权限令牌

**Runner 控制通道（Runner Control Channel）**：
动作网关与沙箱监督器之间经过对等身份验证、限制为当前用户且不监听 TCP 的本地 IPC；它只传递版本化控制消息、宿主签名票据和脱敏执行事件。
_Avoid_：localhost API、工具网络、共享命名管道

**动作沙箱配置（Action Sandbox Profile）**：
由能力票据派生、对单个动作实际实施文件、进程和网络最小权限的 OS 执行约束；后端无法表达时必须拒绝动作。
_Avoid_：Task 级通用权限、执行后检查

**沙箱能力报告（Sandbox Capability Report）**：
工具模式启动前通过 runner 身份校验、能力声明和主动负向探测生成的版本化隔离证据；任一必需能力缺失或探测意外成功都会失败关闭。
_Avoid_：后端版本检查、最佳努力沙箱

**能力切片（Capability Slice）**：
可以独立完成动作网关裁决、OS 强制、结果披露、审计和负向验证后启用的一组能力；未完成切片必须从模型可见能力中隐藏，不能回退宿主执行。
_Avoid_：开发阶段、工具开关、部分权限系统

**能力认证（Capability Certification）**：
能力切片在目标 OS backend 上同时通过授权契约、数据泄露、真实负向探测、控制面攻击、故障恢复、资源限制和 HITL E2E 后获得的可启用状态；未运行、未知、跳过或不稳定都不构成认证。
_Avoid_：测试通过、CI 通过、backend 可用

**失败关闭（Fail Closed）**：
无法建立或维持可验证的 OS 沙箱时拒绝启用或继续执行业务工具，且永不回退到宿主执行。纯文本模式不属于工具执行降级。
_Avoid_：部分沙箱、unsafe 模式

**拒绝结果（Denied Result）**：
动作未获授权时返回给 Agent 的结构化结果；它阻止该动作，但不自动终止任务，Agent 可以据此重新规划。
_Avoid_：任务失败、Agent 停止

**安全完整性故障（Security Integrity Failure）**：
票据、隔离、动作网关或必需安全服务不再可信的故障；它必须终止当前 Run、撤销票据并销毁 Task sandbox，不能作为普通拒绝继续运行。
_Avoid_：权限拒绝、工具失败

**授权审计事件（Authorization Audit Event）**：
保存在工作区外、在票据签发、动作执行或结果披露前持久化、用于解释各层裁决及执行状态的脱敏结构化元数据；它不包含 Prompt、参数正文、文件内容、原始输出或凭据值。
_Avoid_：会话副本、完整内容日志

**执行前审计（Pre-execution Audit）**：
在签发能力票据或沙箱监督器消费 nonce 前必须完成 durable flush 的授权与执行元数据；写入失败会阻止动作并形成安全完整性故障。
_Avoid_：异步日志、执行追踪、最佳努力审计

**结果审计屏障（Outcome Audit Barrier）**：
动作结束后、任何结果释放给 AgentLoop 前必须完成的执行结果元数据持久化屏障；屏障失败会立即收权并报告副作用可能已经发生。
_Avoid_：结果日志、后台 flush、响应后审计

**结果披露动作（Result Disclosure Action）**：
将工具原始结果释放给特定模型或其他出口的独立标准化动作；工具执行获准不代表其结果已获准披露。
_Avoid_：工具执行结果、自动回传

**输入守卫（Input Guard）**：
在上下文片段进入会话历史或模型请求前保持来源、执行数据分类与出口授权，并对最终组装请求再次扫描的宿主边界；未获准的当前用户任务正文会阻止模型调用，原始临时缓冲不得进入历史。
_Avoid_：Prompt 清洗、模型输入过滤器

**输出守卫（Output Guard）**：
在模型流片段进入本地终端、转录或其他出口前进行增量分类与出口授权的宿主边界；未检查内容不能先行显示。
_Avoid_：事后脱敏、模型输出过滤器

**出口授权（Destination-bound Grant）**：
绑定数据分类、来源、目标出口、用途和范围的不可传递授权；一个出口获准不能被解释为其他出口也获准。
_Avoid_：Task 数据通行证、已授权数据

**模型目标（Model Destination）**：
Task 创建时固定的 profile、协议、模型部署和服务 origin 组合，是模型数据出口授权不可替换的目标。
_Avoid_：Provider 类型、备用模型

**Plan 审批（Plan Approval）**：
用户对指定 `planId + version` 进入执行阶段的流程决定，不产生能力票据、任务授权或权限规则，计划中的每个实际动作仍须独立授权。
_Avoid_：授权确认、权限模式
